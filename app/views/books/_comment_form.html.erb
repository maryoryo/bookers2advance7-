<%= form_with(model:[book, book_comment], remote: true) do |f| %>
<%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "form-control", id:"book_comment_form" %>
<%= f.submit "送信する" %>
<% end %>